/* function checkWindowSize(){
    let width = window.innerWidth;

    if(width <= 600){
        let lineMargin = { top: 10, right: 10, bottom: 10, left: 10 }
        let lineWidth = 380 - lineMargin.left - lineMargin.right;
        let lineHeight = 600 - lineMargin.top - lineMargin.bottom;
        
    }

} 

window.addEventListener("resize", checkWindowSize);
checkWindowSize()
 */